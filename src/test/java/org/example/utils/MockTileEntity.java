package org.example.utils;

import org.example.map.EntityType;
import org.example.map.TileEntity;

public class MockTileEntity extends TileEntity {
    public MockTileEntity() {
        super(EntityType.Blank);
    }
}
